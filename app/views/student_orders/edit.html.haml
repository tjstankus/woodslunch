%h2#student_name= "Lunch order for #{@student_order.student.name}"
%h3#month_year= @student_order.first_date_of_month.strftime("%B %Y")

%table#student_order{ :cellpadding => '0', :cellspacing => '0' }
  %tr
    - DayOfWeek.weekdays.each do |day|
      %th= day.name

